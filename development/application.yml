#eurekaHost:  eurekaserver-service
#eurekaHost:  ${eurekaHost:eurekaserver-service}
#eurekaUrl: http://${eurekaHost}:8761/eureka
management:
  endpoints:
   enabled-by-default: false
   web:
      exposure:
        include: 
          -* 

eureka:
  instance:
    hostname: eurekaserver-service
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://eurekaserver-service:8761/eureka 

spring:
  rabbitmq:
    host: mosquito.rmq.cloudamqp.com
    port: 5672
    virtual-host: mnrvwrbj
    username: mnrvwrbj
    password: RnAVRqttZHMeSMmZEZJMfTcf7Wg2ACl6
 #curl -v -H "Content-Type: application/json" -H "X-Event-Key: repo:push" -H "X-Hook-UUID: webhook-uuid"  -X POST  http://localhost:8888/monitor -d  "{\"push\": {\"changes\": []} }" 